{% extends 'base.html.twig' %}

{% block title %}Edit CurrencyExchange{% endblock %}

{% block body %}
    <h1>Edit CurrencyExchange</h1>

    {{ include('currency_exchange/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path(PREFIX ~ 'index') }}">back to list</a>

    {{ include('currency_exchange/_delete_form.html.twig') }}
{% endblock %}
