{% extends 'base.html.twig' %}

{% block title %}Edit BoardTitle{% endblock %}

{% block body %}
    <h1>Edit BoardTitle</h1>

    {{ include(tdir ~ '/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path(prefix ~ 'index') }}">back to list</a>

    {{ include(tdir ~ '/_delete_form.html.twig') }}
{% endblock %}
