{% extends 'base.html.twig' %}

{% block title %}Miembro de Junta Directiva{% endblock %}

{% block body %}
    <h1>Miembro de Junta Directiva</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Surname</th>
                <td>{{ member.surname }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ member.name }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ member.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path(prefix ~ 'index') }}">back to list</a>

    <a href="{{ path(prefix ~ 'edit', {'id': member.id}) }}">edit</a>

    {{ include('members/_delete_form.html.twig') }}
{% endblock %}
