{% extends 'base.html.twig' %}

{% block title %}New Company Category{% endblock %}

{% block body %}
    <h1>Create new Company Category</h1>

    {{ include(tdir ~ '/_form.html.twig') }}

    <a href="{{ path(prefix ~ 'index') }}">back to list</a>
{% endblock %}
