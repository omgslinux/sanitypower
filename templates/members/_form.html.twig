{{ form_start(form) }}
    <div class="row">&nbsp;</div>
    <div class="row">
        <div class="col-6">
        {{ form_row(form.surname) }}
        </div>
        <div class="col-6">
        {{ form_row(form.name) }}
        </div>
    </div>
    <div class="row">
        <div class="col-12">
        {{ form_row(form.notes) }}
        </div>
    </div>
    <button class="btn btn-info">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
