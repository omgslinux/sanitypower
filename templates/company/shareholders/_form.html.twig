{{ form_start(form) }}
    <div class="row">&nbsp;</div>
    <div class="row">
        <div class="col-4">
        {{ form_row(form.holder) }}
        </div>
        <div class="col-4">
        {{ form_row(form.holderCategory) }}
        </div>
        <div class="col-2">
        {{ form_row(form.directOwnership) }}
        </div>
        <div class="col-2">
        {{ form_row(form.totalOwnership) }}
        </div>
    </div>
    <button class="btn btn-info">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
