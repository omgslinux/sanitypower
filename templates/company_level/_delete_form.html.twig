<form method="post" action="{{ path(PREFIX ~ 'delete', {'id': company_level.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_level.id) }}">
    <button class="btn">Delete</button>
</form>
