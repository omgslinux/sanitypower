<form method="post" action="{{ path('board_title_delete', {'id': board_title.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ board_title.id) }}">
    <button class="btn">Delete</button>
</form>
