{{ form_start(form) }}
    <div class="row">&nbsp;</div>
    <div class="row">
        <div class="col-2">
        {{ form_row(form.date) }}
        </div>
        <div class="col-10">
        {{ form_row(form.description) }}
        </div>
    </div>
    <button class="btn btn-info">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}
