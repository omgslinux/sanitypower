{% extends 'base.html.twig' %}

{% block title %}BoardTitle{% endblock %}

{% block body %}
    <h1>BoardTitle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ board_title.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ board_title.name }}</td>
            </tr>
            <tr>
                <th>Alias</th>
                <td>{{ board_title.alias }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('board_title_index') }}">back to list</a>

    <a href="{{ path('board_title_edit', {'id': board_title.id}) }}">edit</a>

    {{ include('board_title/_delete_form.html.twig') }}
{% endblock %}
