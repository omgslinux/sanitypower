{% extends 'base.html.twig' %}

{% block title %}Glossary{% endblock %}

{% block body %}
    <h1>Glossary</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ glossary.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ glossary.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ glossary.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_glossary_index') }}">back to list</a>

    <a href="{{ path('app_glossary_edit', {'id': glossary.id}) }}">edit</a>

    {{ include('glossary/_delete_form.html.twig') }}
{% endblock %}
