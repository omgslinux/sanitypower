{% extends 'base.html.twig' %}

{% block title %}ShareholderCategory{% endblock %}

{% block body %}
    <h1>ShareholderCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shareholder_category.id }}</td>
            </tr>
            <tr>
                <th>Letter</th>
                <td>{{ shareholder_category.letter }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ shareholder_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shareholder_category_index') }}">back to list</a>

    <a href="{{ path('app_shareholder_category_edit', {'id': shareholder_category.id}) }}">edit</a>

    {{ include('shareholder_category/_delete_form.html.twig') }}
{% endblock %}
