<form method="post" action="{{ path('app_glossary_delete', {'id': glossary.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ glossary.id) }}">
    <button class="btn">Delete</button>
</form>
